Пояснительная записка:
Задача 1:

1)Создан класс Grid, который описывает структуру сетки;
2)Реализован экземплярный метод GetGridPassing, который принимает на вход поле и возвращает булево значение:
 можно ли пройти поле сверху вниз, двигаясь только влево, вправо и вниз в пределах свободных ячеек;
3)Созданы также:
 - экземплярный метод SetGridRandom, заполняющий поле случайными значениями;
 - экземплярный метод GetGridConsole для вывода содержимого сетки на консоль.
 Методы для тестирование:
 - статический метод Test_GetGridRandomPassing, создающий случайное поле и тестирующий его на проходимость;
 - статический метод Test_GetTrueGridPassing, создающий поле, которое может быть пройдено;
 - статический метод Test_GetFalseGridPassing, создающий поле, которое не может быть пройдено.
4) Оценить сложность алгоритма не смог, вследствие незнания и ограничения по времени выполнения задания.

Задача 2:

1)Код для создания искомой таблицы на T-SQL:

CREATE TABLE Staff
(
	ID INTEGER IDENTITY Primary KEY,
	Departmentid INTEGER NOT NULL,
	NAME NVARCHAR(35) NOT NULL,
	SALARY SMALLMONEY
)

2)Запросы на T-SQL:

- Количество сотрудников по каждому подразделению организации:

SELECT COUNT(*) AS Staff_Count
FROM dbo.Staff
GROUP BY Departmentid

- Имена сотрудников и их зарплата, при этом зарплата таких сотрудников должна быть выше средней зарплаты по всей организации:

SELECT Name, Salary
FROM dbo.Staff
WHERE (Salary > (SELECT AVG(Salary) FROM dbo.Staff))

- Все подразделения и первое наиболее часто встречающееся имя сотрудника в каждом подразделении, количество сотрудников с таким именем в подразделении:
 На данный момент решения не придумал.